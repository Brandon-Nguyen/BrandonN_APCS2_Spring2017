package textExcel;

public class PercentCell extends RealCell{
	
	private String percent;
	
	public PercentCell(String percent) {
		// TODO Auto-generated constructor stub
		this.percent = percent;
	}

	public double getDoubleValue(){
		String returnVal = percent.substring(0, percent.length() - 1);
		System.out.println(returnVal);
		System.out.println(Double.parseDouble(returnVal) / 100.0);
		return Double.parseDouble(returnVal) / 100.0;
	}
	
	public String abbreviatedCellText(){
		String returnVal = percent;
		if(returnVal.length() > 10){
			returnVal = returnVal.substring(0, 10);
			return returnVal;
		}else{
			for(int i = 0; i < 10 - returnVal.length(); i++){
				returnVal += " ";
			}
			return returnVal;
		}
	}
	
	public String fullCellText(){
		return percent;
	}
}